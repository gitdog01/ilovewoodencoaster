# CLAUDE.md — 프로젝트 인수인계

## 한 줄 요약
OpenRCT2용 "우든 롤러코스터 트랙 생성 플러그인"을 만들고, 그 안에 들어갈
작은 트랜스포머(sLLM)를 직접 학습시킨다.

## 핵심 설계 결정 (이미 내린 것들)

1. **LLM 스케일이 아니다.** 트랙 = 어휘 100 미만, 길이 100~400의 토큰 시퀀스.
   nanoGPT급(5~10M 파라미터)이면 충분. 로컬 NVIDIA GPU로 몇 시간.

2. **게임을 라벨러로 쓴다.** 공개된 우든 코스터 디자인은 수천 개 수준이라
   부족하다. 절차적 생성기로 유효 트랙을 대량 생성하고 OpenRCT2가
   흥미도/격렬도/멀미도를 매기게 한다. 사람 디자인은 스타일 파인튜닝용 소량.

3. **조건부 생성 (Decision Transformer 방식).** 시퀀스 앞에 목표 지표와
   부지 제약을 버킷 토큰으로 붙여 학습한다.
   `[BOS][exc=8][int=6][cost=2][width=12][station=4][SEP] <트랙> [EOS]`
   추론 시 유저가 플러그인 UI에 넣은 값을 그대로 프리픽스로 사용.

4. **폐곡선/충돌은 학습으로 해결 안 된다.** constrained decoding 필수.
   매 스텝 geom/simulator.py 로 불가능한 토큰을 마스킹한다.

5. **최종 형태는 LM proposal + best-of-N.** 여러 후보를 뽑아 게임으로 검증하고
   최고를 고른다. 플러그인 UX로도 "후보 3개 제시"가 자연스럽다.

## 의존하는 외부 물건
- OpenRCT2 0.5.0 이상 (quickjs-ng 스크립팅 엔진 필요)
- `markusklock/openrct2-ridecreation-api` 플러그인 — TCP JSON API.
  **패치된 정본이 리포의 `plugin/ridecreation-api.js` 에 있다** (아래 패치 항목 참고).
  포트는 **인스턴스마다 고정**이다 (원본의 빈 포트 자동 탐색은 Windows에서
  동작하지 않는다 — 아래 "병렬 수집" 참고). 단일 인스턴스면 8080.
- Windows에서 플러그인 로그를 보려면 `openrct2.exe`가 아니라 `openrct2.com` 실행.
  플러그인은 타이틀 화면이 아니라 **공원 로드 후** 시작된다.

## 알려진 제약 / TODO
- 플러그인이 노출하는 조각에 **헬릭스, 브레이크, S-bend, 워터스플래시가 없다.**
  우든 코스터 평점에 꽤 영향이 크므로 5단계에서 플러그인을 포크해 확장해야 함.
- 자기충돌 검사는 `geom/planner.py`의 `Occupancy` + `_footprint_deltas()`가
  맡는다. `getAllTrackSegments`가 조각의 타일 블록 목록을 안 내려주므로,
  턴 조각은 **진입/진출 타일의 바운딩 박스**로 보수적으로 잡는다 (5칸 턴이면
  실제 7블록 대신 11칸). 실제보다 더 잡지만 덜 잡아서 게임이 배치를 거부하는
  것보다 낫다. 정확한 블록 목록을 실측해 넣으면 배치 밀도가 더 올라간다 (TODO).
  - 주의 1: 점유 타일에서 **진입 타일 (0,0,0)은 반드시 빼야 한다.** 그 자리는
    직전 조각이 정상적으로 공유하는 타일이라, 넣어두면 모든 턴이 항상 충돌로
    걸려서 트랙에 턴이 하나도 안 나온다 (한 번 당했음).
  - 주의 2: z를 정확히 비교하면 안 된다. 게임이 트랙 위아래 여유를 요구해서
    한두 칸 차이로 겹친 트랙도 거부한다. `Occupancy(ztol=2)`가 이걸 본다.
- 격렬도가 10을 넘으면 손님이 안 탄다. 흥미도 최대화가 아니라 **제약 하 최적화.**
- 평점은 주변 주경, 근처 동일 라이드 등에 영향받는다. 라벨링은 반드시 빈 평지에서.

## 진행 상황
- [x] 0단계: OpenRCT2 + 플러그인 설치, 공원 로드
- [x] 0단계 확인: `python scripts/00_smoke.py`
- [x] 2단계: `python scripts/01_extract_geometry.py` -> geometry.json (652개 조합, origin z=30
      + UP25/DOWN25류 16개는 실측값으로 수동 보충, 아래 TODO 참고)
- [x] 1단계: `python scripts/02_hello_coaster.py` -> 평점 출력 (흥미 0.27/격렬 0.30/멀미 0.18)
- [x] 3단계: 2026-09-05~06 수집 완료. **`data/dataset.jsonl` 에 108,296개**
      (중복 13,225개 제거 후), 학습용 gen2+ 가 107,431개.
      인스턴스 25개 병렬로 시간당 약 7000개. 아래 "수집 결과" 참고.
- [x] 4단계: 학습 + 조건부 생성이 종단으로 돈다.
      - `scripts/04_train.py` -> 4.79M 파라미터, RTX 5070에서 **1.2분**, val 0.6365
      - `model/constrain.py` constrained decoding -> 부지 준수 12~20% -> **100%**
      - `model/hybrid.py` LM 제안 + A* 마무리 -> 폐곡선 0% -> **100%**
      - `model/bestof.py` + `scripts/07_bestof.py` best-of-N (설계 결정 5)
      - **게임 종단 검증**: 요청 exc/int/latg 2.5/2.0/1.2 -> 실측 1.90/2.10/1.15,
        5.5/8.0/3.0 -> 5.02/7.05/2.92. 요청한 목표값을 실제로 맞춘다.
      - 2026-09-12 재학습: **val 은 좋아졌는데 게임 성능은 안 좋아졌다.** 아래
        "재학습 결과" 참고. `model/ckpt.pt` 는 **바꾸지 않았다** (옛 33,171개
        학습본이 게임 검증에서 아직 제일 낫다).
- [~] 5단계: 플러그인 UI 첫 버전이 돈다 (지도 메뉴 -> "우든 코스터 생성기").
      `scripts/08_daemon.py` 를 띄워둬야 한다. 거친 부분은 "바로 다음 할 일" 참고.

## 조건 토큰 8개 중 2개가 죽어 있다 (2026-09-12 실측)
`python scripts/check_buckets.py` 로 아무 때나 다시 볼 수 있다. 108,296개 기준:

| 조건 | 쓰는 버킷 | 비고 |
|---|---|---|
| exc / int / nau / latg | 4/4, 6/6, 6/6, 7/7 | 다 살아 있다 |
| width | 3/7 | 20 미만이 데이터에 없다 |
| depth | 4/7 | 16 미만이 없다 |
| **height** | **1/6** | 100% 한 버킷. 정보 0 |
| **station** | **1/6** | 100% 한 버킷. 정보 0 |

원인은 `03_collect.py` 의 `sample_config()` 가 높이를 `--height` 고정값으로,
스테이션을 3으로만 쓰기 때문이다. 흔들지 않으면 조건이 될 수 없다.
**그래서 UI 에서 그 둘을 조절해도 모델은 반응할 방법이 없다.**

당장 사고가 될 뻔한 것: UI 스피너는 부지를 12까지 내려주는데 모델은 width>=20,
depth>=16 만 본 적이 있어서, 작은 부지를 넣으면 **학습된 적 없는 조건 토큰**이
프리픽스에 들어갔다 (임베딩이 초기값 그대로 -> 출력이 조용히 망가진다).
`model/tokenizer.py` 의 `SEEN_BUCKETS` 가 추론 때 학습된 범위로 눌러서 막는다.
부지 제약 자체는 constrained decoding 이 기하로 강제하므로 작은 부지에 짓는 건
그대로 된다. **수집기가 범위를 넓히면 `check_buckets.py` 를 다시 돌려
`SEEN_BUCKETS` 를 고칠 것.**

## 알려진 제약 / TODO 추가
- **설치된 `markusklock/openrct2-ridecreation-api`에 `segment.getNextValidSegments`가 없어서
  `placeTrackPiece`/`getValidNextPieces` 응답이 항상 "not a function"으로 깨지는 버그가 있었음.**
  로컬 설치본(`Documents/OpenRCT2/plugin/ridecreation-api.js`)의 `computeValidNextPieces`를
  직접 패치해서 해결 (해당 함수 존재 여부를 체크하고 없으면 validPieces만 빈 배열로 처리,
  좌표(position/nextEndpoint)는 그대로 반환). 원본은 `ridecreation-api.js.bak`으로 보관.
- **플러그인 패치 정본은 이제 리포의 `plugin/ridecreation-api.js` 다.** 패치가 두 개
  쌓였고(위의 `getNextValidSegments` 가드 + 포트 고정), 플러그인을 재설치하면 둘 다
  날아가기 때문에 버전 관리되는 곳에 옮겼다. 재설치했다면 이 파일을
  `Documents/OpenRCT2/plugin/` 에 덮어쓰고, 병렬용 사본은
  `python scripts/setup_instances.py --n 4` 로 다시 만들면 된다.
- 포트 고정 패치: 원본의 "8080부터 빈 포트 탐색" 루프를 `server.listen(DEFAULT_PORT)`
  하나로 바꿨다. 이유는 아래 "병렬 수집" 참고 (Windows SO_REUSEADDR).
- `rct/client.py`의 `place()`는 이제 조각의 `beginZ`(진입 z 오프셋, 8단위=tileCoordinateZ 1칸)를
  자동 보정한다. 내리막류 조각은 baseZ가 슬로프 낮은 쪽 기준이라 이 보정이 필요함.
- `01_extract_geometry.py`는 **항상 평지 스테이션 바로 다음에서만** 각 조각을 테스트하므로,
  UP25/DOWN25처럼 같은 경사 상태의 조각 뒤에만 이어질 수 있는 "중간 경사" 조각들은
  구조적으로 못 잡는다 (게임 룰이지 버그 아님). 636/1400개가 현재 방식의 사실상 상한.
  더 채우려면 조각별로 적절한 전환 조각을 먼저 놓고 테스트하는 다단계 추출이 필요함 (TODO).
- extract origin은 지면(z=14)보다 너무 높이 띄우면 "지지대 최대 높이" 제한에 걸린다.
  현재 z=30 (지면+16) 사용 중 — 내리막 조각이 땅에 안 박힐 정도로만 살짝 띄운 값.

## 다음에 할 일 (여기서부터 이어서)
0~2단계 끝났고, **3단계 수집 파이프라인이 실제로 돈다.** 이번에 구조를 갈아엎었다.

### 이번에 바뀐 것 (왜 바꿨는지)
예전 `gen/random_walk.py`는 **게임 안에서** 조각을 하나씩 놓아보며 무작위로
헤맸다. 폐곡선이 되려면 스테이션 진입점에 (x, y, z, 방향, 경사, 뱅크) 6개가
전부 일치해야 하는데 무작위 워크로 거기 우연히 도달할 확률은 사실상 0이라,
`--n 3` 을 돌리면 3/3 폐곡선 실패였다.

`geometry.json` 에 조각마다 begin/endSlope, begin/endBank 가 들어있다는 걸
발견해서(= 게임의 조각 연결 규칙을 오프라인에서 그대로 재현 가능),

- `geom/planner.py` (신규): 상태를 (x, y, z, 방향, 경사, 뱅크) 6-튜플로 두고
  A* 로 스테이션까지 **정확히** 닫는 조각열을 찾는다. `wander()` 는 무작위
  워크, `plan()` 은 폐곡선 마무리.
- `gen/random_walk.py`: 리프트 언덕 -> 첫 낙하 -> 무작위 본체 -> A* 복귀 순으로
  **설계를 전부 파이썬에서 끝내고**, 완성된 시퀀스만 게임에 한 번 짓는다.
  실패한 설계는 게임 왕복이 없으니 사실상 공짜.
- `geom/simulator.py`: `Bounds.plot()` 추가. 기존 `around()` 는 부지를 스테이션
  중심으로 잡는데, 트랙은 스테이션이 보는 방향으로만 뻗으므로 리프트 언덕이
  곧장 부지 밖으로 나가버렸다. `plot()` 은 스테이션을 부지 가장자리에 둔다.
- `rct/client.py`: `set_game_speed()` 추가. 평점은 시뮬 시간 ~35초가 지나야
  나오는데 이게 전체 시간의 대부분이었다. **속도 8이면 트랙당 30초 -> 0.4초.**
- `geometry.json`: 수동 보충한 16개 조합(UP25/UP25_TO_FLAT/DOWN25/DOWN25_TO_FLAT)의
  slope/bank 가 null 이라 planner 가 못 쓰던 걸 채웠다 (0=평지, 2=up25, 6=down25).
- `scripts/03_collect.py`: 에피소드마다 부지/리프트/길이를 무작위로 흔든다.
  한 설정으로만 뽑으면 평점이 E 0.5 부근에 뭉쳐서 조건부 생성 학습에 쓸 게 없다.

### 지금 성능
`--n 30` 기준 11/30 성공, 트랙당 5~10초, E 0.58~5.15 / I 0.71~6.92 / N 0.40~3.74.
(예전: 0/3 성공, 트랙당 33초, E 0.27~1.19)

### 바로 다음 할 일
1. ~~본 수집~~ 완료. 2026-09-05 하루에 **3만 개 이상** 모았다 (인스턴스 25개
   병렬, 시간당 약 7600개). `python scripts/build_dataset.py --min-gen gen2` 로
   `data/dataset.jsonl` 을 만든다.
2. ~~데이터 쏠림(맨턴 74%, 격렬도 10 미만) 해소~~ 완료 — gen2 의 `strict_banked`
   와 리프트 10~13 개방으로 해결했다. gen2/gen3 에서는 banked=True 의 맨턴이
   중앙 1개, 격렬도 10 초과가 약 5% 나온다.
3. **4단계 결과가 나쁠 때 데이터를 더 뽑는 절차.** 추가 수집 자체는 명령 두 줄이다
   (`run_instances.py` -> `collect_all.py`, `--seed-base` 만 안 쓴 값으로). 시간당
   7600개라 1만 개 추가에 1.3시간. **다만 "더 많이"보다 "다르게"인 경우가 많다:**

   a. 모델이 못 맞추는 요청을 특정한다 (예: "격렬도 6에 흥미도 4").
   b. **그 구간이 데이터에 있는지 먼저 본다.** 없으면 아무리 더 뽑아도 소용없다.
      실제로 gen1 946개는 격렬도가 10을 한 번도 안 넘어서, 10만 개를 뽑았어도
      "격렬도 10 제한"을 배울 근거가 없었다. `sample_config()` 를 고쳐야 나왔다.
   c. 없으면 `03_collect.py` 의 `sample_config()` 를 그 구간 쪽으로 편향시켜서
      표적 수집한다. 생성기를 고쳤으면 `build_dataset.py` 의 `GENERATIONS` 에
      새 세대를 추가할 것.

4. ~~4단계 학습 코드~~ 완료. 좌우G 버킷도 넣었다 — 검토해보니 **유일하게 연속인
   지표**라(다른 건 덩어리로 뭉친다) 해상도가 제일 높고, 게임 검증에서도 요청값을
   제일 정확히 맞춘 게 좌우G였다.

## 재학습 결과 (2026-09-12) — 다음에 학습을 건드리기 전에 읽을 것

107,431개(gen2+)로 크기를 네 가지 학습하고, **게임으로 직접 비교했다**
(목표 3종 x 후보 12개, 같은 시드, `--build 8`). 점수는 `model/bestof.py` 의
`score()` — 요청값과의 거리에 흥미도 보너스, 높을수록 좋다.

| 체크포인트 | 파라미터 | val | 폐곡선 | 배치 | 평균점수 | \|I오차\| |
|---|---|---|---|---|---|---|
| `ckpt.pt` (옛 33,171개) | 4.79M | 0.6365 | 100% | 71% | **-0.26** | 1.05 |
| sweep_B 4층x256 | 3.21M | 0.6000 | 100% | 62% | -0.51 | 1.25 |
| sweep_A 6층x256 | 4.79M | 0.5870 | 100% | 71% | -0.51 | 1.19 |
| sweep_C 8층x384 | 14.28M | 0.5691 | 100% | 71% | -0.49 | 1.15 |
| sweep_D 12층x512 | 37.94M | 0.5531 | 100% | 58% | -0.47 | 1.12 |

**val 손실과 게임 성능이 어긋난다.** 데이터 3.2배 + 파라미터 8배로 val 은
0.6365 -> 0.5531 (13% 개선) 인데, 정작 "요청한 지표를 맞추는가"는 옛 모델이
제일 낫다. 재학습군 안에서는 클수록 val 도 점수도 단조 개선이지만, 넷 다
옛 모델에 못 미친다. 그래서 **`model/ckpt.pt` 는 안 바꿨다.** 스윕 체크포인트는
`model/sweep_*.pt` 로 로컬에 남아 있다 (`.gitignore` 대상).

**원인은 모델이 아니라 버킷 해상도다.** 차이는 전부 고난도 목표
(E5.5/I8.0/G3.0) 한 곳에서 났다 — 옛 모델 I=7.18, 새 모델들 I=9.1~9.3.
그런데 둘 다 `<int=4>` = 격렬도 `[7.0, 10.0)` 버킷 **안**이라 모델 입장에서는
양쪽 다 정답이다. 버킷 폭이 3이나 된다.

더 나쁜 건 그 버킷 **안이 쌍봉**이라는 것이다 (gen2+ 12,082개):

| p10 | p25 | p50 | p75 | p90 | 평균 |
|---|---|---|---|---|---|
| 7.06 | 7.20 | 9.01 | 9.28 | 9.44 | 8.42 |

7.0~7.2 덩어리와 9.0~9.4 덩어리뿐이고 **8 근처가 비어 있다.** 즉 "격렬도 8"은
데이터에 존재하지 않아서 원리적으로 못 맞춘다. 위 표의 `|I오차| ~ 1.1` 은
모델 한계가 아니라 **데이터가 만든 바닥**이고, 이 벤치마크는 그 아래를
구분하지 못한다 (모델 간 차이는 사실상 버킷 안에서의 운).

=> 다음에 할 것은 모델을 더 키우는 게 아니라 **(a) `COND_SPECS["int"]` 를
7~10 구간에서 쪼개고 (b) 그 구간 사이를 메우는 표적 수집** 이다. 8 부근 트랙이
실제로 존재하는지부터 봐야 한다 (없으면 쪼개도 빈 버킷만 생긴다 -- "바로 다음
할 일" 3번 절차 그대로).

### 2026-09-12 시점의 다음 할 일
1. ~~재학습~~ 했다. 위 "재학습 결과" 참고. 결론은 **모델 크기가 병목이 아니다.**
   다음은 격렬도 버킷 쪼개기 + 그 구간 표적 수집.
2. **UI 거친 부분** (`scripts/08_daemon.py` + 플러그인 UI):
   - ~~`env.reset()` 이 공원의 기존 라이드를 전부 지운다~~ 고쳤다.
     `reset(clear=...)` 가 `"all"`(수집기) / `"own"`(유저 공원) / `None` 으로
     갈린다. 데몬은 `"own"` 을 쓴다. 플러그인에 `deleteRide` 엔드포인트를
     새로 넣었다 (기존엔 전체 삭제밖에 없었다). 실측으로 확인: 다른 자리에
     지어둔 유저 라이드가 후보 채점 3회를 거쳐도 그대로 남는다.
   - ~~데몬을 안 띄우면 버튼이 반응 없어 보인다~~ 고쳤다. 데몬이 매초
     `getGenerationRequest` 를 폴링하므로 그 틱을 기록해두고, 5초 넘게
     소식이 없으면 "데몬이 안 보입니다" 로 알려준다.
   - ~~데몬이 붙으면 공원이 영구히 8배속~~ 고쳤다. 요청을 처리하는 동안만
     8배속이고 끝나면 `--idle-speed`(기본 1)로 되돌린다.
   - **좌표가 origin (67,66) 고정** -- 아직 남았다. 그래서 완성품을 공원에
     남기면 그 자리가 막혀 **다음 요청이 스테이션을 못 깐다** (실측 확인).
     기본 동작은 "자기 트랙 한 개를 계속 갈아끼움"이고, 남기고 싶으면
     `--keep` 인데 그러면 1회용이다. 자리 선택이 들어가야 풀린다.
   - 스피너가 정수만 받는다 (좌우G 는 x10 으로 우회 중).
3. **조각 어휘 47% 공백.** RCT2 스톡 우든 코스터 30개(조각 3845개)를 뜯어보니
   생성기 어휘(21종)로 커버되는 게 53.1% 뿐이다 (`geometry.json` 기준으로도
   69.7%). 자주 쓰는데 못 만드는 것: 브레이크(76회), 급브레이크(35회),
   헬릭스류(150회+), S-bend(13회), 60도 경사 계열(640회).
   **행동 공간에 없는 수는 아무리 탐색해도 안 나온다.** 지금 모델은 절반의
   어휘로 만든 것이고, 나중에 늘리면 재학습이 필요하다.

## 평점이 어떻게 정해지는가 (실측)
`rct/client.py`의 `measurements()`(플러그인 `getRideMeasurements`)가 평점 3개
말고도 **좌우G/수직G/에어타임/최고속도/낙하 수/최고낙하높이**를 내려준다.
`evaluate()`는 이걸 통째로 반환하고 수집 데이터에도 그대로 들어간다.
격렬도가 왜 튀었는지는 평점 3개만 봐서는 절대 알 수 없으니 여기를 봐야 한다.

> **주의: 아래 1~4는 트랙 수십 개짜리 소표본에서 뽑은 관찰이다.**
> 2026-09-05에 946개를 모으고 다시 재보니 1번과 2번이 그대로 성립하지 않았다.
> 맨 아래 "946개 본 수집 결과"를 먼저 볼 것.

**1. 리프트 높이 = 최고속도 = 흥미도.** 거의 직결이다.

| 리프트 | 최고속도 | 최고낙하 | 흥미도 |
|---|---|---|---|
| 3 | 23~26 | 6~8 | 0.3~1.3 |
| 6~7 | 32~36 | 12~16 | 2.4~4.9 |
| 8~9 | 38~40 | 18~20 | 5.0~5.2 |

**2. 뱅크(커빙) 없는 턴이 격렬도의 주범.** 같은 리프트 높이(8~9, 속도 38~40)에서:

| 턴 구성 | 좌우G | 격렬도 | 흥미도 |
|---|---|---|---|
| 맨턴 19 / 뱅크 3 | 3.02 | 9.22 | 5.21 |
| 맨턴 16 / 뱅크 2 | 2.52 | 6.87 | 5.06 |
| 맨턴 2 / 뱅크 10 | 1.67 | 3.09 | 2.60 |
| 맨턴 0 / 뱅크 16 | 0.96 | 2.81 | 2.60 |

즉 **뱅크는 격렬도를 흥미도와 맞바꾸는 레버다.** 격렬도 10 제한 아래에서
흥미도를 최대화하는 게 목표이므로 이 레버가 정확히 필요한 물건이다.
`gen/random_walk.py` 의 `WEIGHTS`(뱅크 선호) / `WEIGHTS_PLAIN`(맨턴 선호)를
`banked` 플래그로 골라 쓰고, `03_collect.py` 가 에피소드마다 50%로 뽑는다.
양쪽 영역이 다 있어야 조건부 생성이 "격렬도 6짜리 흥미도 최대"를 배운다.

**3. 리프트를 너무 높이면(10칸 이상) 흥미도는 안 오르고 격렬도만 10~15로
폭주한다.** 한 방에 떨어지는 직선 낙하라 G가 튄다. `sample_config()` 의
`lift` 를 3~9로 제한해둔 이유.

**4. 부지 끝에서 벽을 마주보면 그대로 막다른 길.** RCT 조각은 예외 없이 최소
한 칸 전진해서 제자리 회전이 없기 때문. `_exits()` 로 착지 지점에 여유를 요구한다.

## 수집 결과 (2026-09-06 기준) — 4단계 전에 읽을 것
`data/dataset.jsonl` **108,296개** (중복 13,225개 제거 후), 학습용 gen2+ 107,431개.
112MB. 세대별 분포는 `python scripts/build_dataset.py` 가 찍어준다.

| 지표 | 최소 | 중앙 | 최대 |
|---|---|---|---|
| 흥미도 | 0.28 | 2.40 | 6.00 |
| 격렬도 | 0.31 | 3.19 | 14.74 |
| 멀미도 | 0.17 | 1.77 | 9.05 |
| 좌우G | 0.64 | 2.29 | 3.53 |
| 최고속도 | 22 | 35 | 47 |

조각 수 21~109. **격렬도 10 초과가 5,263개(4.9%)** 라 "격렬도 10 제한" 경계가
데이터에 충분히 들어있다 (gen1 946개에는 하나도 없었다).

`banked` 레버는 대량에서도 유지된다 (gen4 73,143개 기준):

| banked | n | 맨턴 중앙 | 좌우G | 격렬 |
|---|---|---|---|---|
| True | 37,913 | 1.0 | 1.94 | 3.05 |
| False | 35,230 | 14.0 | 2.48 | 3.33 |

아래 소표본 관찰(946개)은 **기록용으로 남겨둔다.** 1번(리프트 포화)은 대량에서도
성립했고, 2번(banked 무효)은 gen2 에서 고쳤다.

<details>
<summary>946개 시절 관찰 (gen1)</summary>

| 지표 | 최소 | 중앙 | 최대 |
|---|---|---|---|
| 흥미도 | 0.28 | 2.36 | 5.76 |
| 격렬도 | 0.31 | 2.96 | 9.67 |
| 멀미도 | 0.18 | 1.69 | 5.70 |
| 좌우G | 0.78 | 2.25 | 3.02 |
| 최고속도 | 22 | 34 | 40 |

조각 수 21~95. **격렬도 10을 넘는 트랙이 하나도 없다** — 제약이 한 번도 안 걸렸다.

**소표본 관찰과 어긋난 것 두 가지 (둘 다 4단계 설계에 영향):**

1. **리프트 흥미도는 6에서 포화된다.** 위 표 1은 리프트 8~9에서 흥미 5.0~5.2라고
   했지만, 946개 중앙값은 리프트 6/7/8/9가 각각 2.45/2.47/2.40/2.47로 **평평하다**
   (3→0.60, 4→1.19, 5→1.26 까지만 오른다). 흥미 5점대는 최대값이지 전형값이 아니다.
   `sample_config()` 가 리프트를 3~9로 흔들어도 6 이상은 사실상 같은 구간을 다시
   뽑는 셈이라, 조건부 프리픽스가 배울 게 이 범위에는 별로 없다.

2. **`banked` 플래그가 의도한 두 영역을 못 만든다.** 표 2는 맨턴 19/뱅크 3 과
   맨턴 0/뱅크 16 을 좌우G 3.02 대 0.96 으로 갈랐지만, 실제 수집물은:

   | banked | n | 맨턴 중앙 | 뱅크턴 중앙 | 좌우G 중앙 | 격렬 중앙 |
   |---|---|---|---|---|---|
   | False | 470 | 13.5 | 0 | 2.34 | 3.12 |
   | True | 476 | 9.0 | 4 | 2.15 | 2.91 |

   `banked=True` 를 줘도 맨턴이 중앙 9개나 남는다. 다만 **중앙값이 실상을 가린다.
   banked=True 의 맨턴 분포는 쌍봉이다**: 0~4개인 트랙이 207개, 9개 이상이 226개로
   두 덩어리로 갈린다.

   원인은 `plan_episode` 의 닫기 단계다. `banked=True` 면 먼저 `exclude=PLAIN_TURNS`
   로 맨턴 없이 닫아보고, 실패하면 맨턴을 허용해 다시 부른다. **앞쪽이 성공하면
   맨턴이 0~2개로 깨끗하게 나오고, 폴백으로 넘어가면 tail 전체(close_budget 20~30
   조각)가 맨턴 위주가 된다.** 실측상 폴백이 절반쯤 뜬다 (오프라인 재현: 맨턴 금지
   A\*가 77/120 성공, 폴백 43/120). 무작위 워크 구간 자체는 맨턴을 거의 안 쓴다
   (중앙 1개) — `WEIGHTS` 는 잘 먹고 있다.

   결과적으로 946개 중 **맨턴 0개인 트랙은 40개(4%)뿐이고 9개 이상이 701개(74%)** 라
   데이터가 한쪽에 쏠려 있다.
   맨턴 수로 다시 가르면 신호 자체는 살아 있다 (0개→좌우G 1.21/격렬 1.39,
   9개+→2.35/3.17). 즉 **레버는 진짜인데 생성기가 그 레버를 못 당기고 있다.**

2026-09-05에 (b)로 갔다. `strict_banked` 로 맨턴 폴백을 막고 리프트 10~13을
열었더니 위 표대로 두 영역이 확실히 갈렸다 (gen2).

</details>

시작 전 체크리스트:
- 병렬로 돌릴 거면 `python scripts/run_instances.py --n 4` 하나면 끝난다
  (실행 + 공원 자동 로드 + 포트가 갈렸는지 확인까지). 단일이면 `openrct2.com`으로
  직접 실행하고 평지 샌드박스 공원 로드.
- 플러그인 패치는 `Documents/OpenRCT2/plugin/` 과 각 인스턴스 폴더에 적용돼 있다.
  플러그인을 재설치했다면 리포의 `plugin/ridecreation-api.js` 로 덮어쓰고
  `setup_instances.py` 를 다시 돌릴 것.

콘솔 인코딩 주의: 한국어 Windows 콘솔은 cp949라 **em-dash(`—`) 같은 문자를
`print()` 하면 `UnicodeEncodeError`로 스크립트가 통째로 죽는다.** 실제로
`03_collect.py` 의 실패 분기 em-dash 하나 때문에 수집이 몇 초 만에 죽었다
(성공 분기에는 없어서 첫 실패까지는 멀쩡해 보인다). 출력 문자열에는 ASCII 문장부호만
쓰고, 오래 도는 스크립트에는 `sys.stdout.reconfigure(errors="replace")` 를 걸어둘 것.

## 병렬 수집 (여러 OpenRCT2 인스턴스)

### 창을 그냥 여러 개 띄우면 안 된다 (2026-09-05 실측)
플러그인 원본은 8080부터 올라가며 빈 포트를 찾게 돼 있지만 **Windows에서는
전부 8080에 겹친다.** OpenRCT2가 리슨 소켓에 `SO_REUSEADDR`를 켜는데, Windows의
`SO_REUSEADDR`는 리눅스와 달리 **다른 프로세스가 이미 리슨 중인 포트에 대한
bind를 그대로 허용한다.** 예외가 안 나니 탐색 루프가 한 번도 위로 안 올라간다.
인스턴스 3개를 띄웠더니 netstat에 8080이 PID 3개로 찍혔다.

이 상태로 `--port 8080/8081/8082`를 돌리면 8081·8082는 연결 실패하고 세 클라이언트가
**같은 게임 하나**에 붙는다. `env.reset()`이 `delete_all_rides()`를 부르므로 서로의
트랙을 지운다 — 속도 이득 0에 데이터 오염.

게임 안에서 클라이언트 소켓으로 미리 찔러보는 우회도 안 된다.
`network.createSocket()`의 connect 콜백이 이 빌드에서 안 불린다 (프로브가 항상
타임아웃 -> "비었네" 오판). 그래서 **포트를 인스턴스마다 못 박는 방식**을 쓴다.

### 세팅 (한 번만)
```bash
python scripts/setup_instances.py --n 4
```
`~/Documents/rct-instances/inst0..3` 을 만든다. 각 폴더는 OpenRCT2의
`--user-data-path` 로 물릴 유저 데이터 폴더이고, 그 안의 플러그인 사본에
`DEFAULT_PORT`가 8080/8081/8082/8083으로 각각 박혀 있다. 무거운 공용 자산
(scenario, object, objdata, track 등)은 복사가 아니라 **정션**이라 원본 샌드박스
맵을 고치면 인스턴스가 자동으로 따라온다 (정션은 관리자 권한 불필요).
덤으로 `debug_replay.parkrep` / autosave 를 인스턴스끼리 겹쳐 쓰던 것도 사라진다.

플러그인 정본을 고쳤으면 사본에도 반영해야 한다. **`--plugin-only` 를 쓸 것:**
```bash
python scripts/setup_instances.py --plugin-only --n 25
```
플러그인 파일만 정본으로 덮어쓰고 포트는 인스턴스별로 다시 박는다. 폴더와
정션은 안 건드린다 (`--force` 는 인스턴스를 통째로 지우고 다시 만든다).
`Documents/OpenRCT2/plugin/` 쪽은 따로 복사해야 한다. 어느 쪽이든 **떠 있는
인스턴스는 재시작해야 반영된다.**

### 실행
```bash
python scripts/run_instances.py --n 4
```
4개를 각각 자기 콘솔 창에 띄우고 **공원까지 자동으로 로드한 뒤**
(`scenario/내 새 시나리오.park`), netstat으로 포트->PID 표를 찍어서 실제로
갈렸는지 보여준다. 포트 하나에 PID가 둘 이상 나오면 그게 위의 중복 bind 사고다.
띄우지 않고 지금 상태만 보려면 `--check`, 공원을 직접 로드하려면 `--no-park`.

검증됨 (2026-09-05): inst1이 8081을 잡고 5초 만에 공원 로드 완료. 8080에 라이드
0개인 상태에서 8081에 스테이션을 놓으니 8081만 1개 — 서로 다른 게임이 맞다.

### 수집
수집기는 인스턴스 하나에 하나씩 붙인다. 25개를 터미널로 띄울 순 없으므로:

```bash
python scripts/collect_all.py --n 25 --each 5000 --seed-base 300 --tag run3
```
각 수집기는 트랙 하나 끝날 때마다 자기 part 파일에 append 하므로 **아무 때나
끊어도 모은 건 남는다.** 그래서 `--each` 는 넉넉히 잡고 원하는 만큼 모이면
끊는 쪽이 편하다. 진행률은 30초마다 찍는다.

수동으로 붙이려면 `03_collect.py --port 8080 --seed 0 --n 250 --out ...`.
**병렬일 때 `--port` 는 필수다** (생략하면 `discover()` 가 8080부터 훑어서 전부
같은 인스턴스에 붙는다). 단일 인스턴스면 생략해도 된다.

**`--seed-base` 는 안 쓴 값으로 줄 것.** 시드가 겹치면 똑같은 트랙이 다시 나온다.
이미 쓴 시드: 0~13, 20~27, 40~45, 60~65, 80~83, 90~95, 200~224, 300~324,
400~424, 999(검증용). 다음엔 500부터.

### 데이터 합치기 — `cat` 하지 말 것
```bash
python scripts/build_dataset.py --min-gen gen2   # -> data/dataset.jsonl
```
세대 태깅 + 중복 제거 + 세대별 분포 리포트를 한다. `cat` 으로 합치면 아래
세대 문제가 그대로 섞여 들어간다.

**지금 있는 `data/dataset.jsonl` 은 `--min-gen` 없이 만든 것이라 gen1 865개가
섞여 있다.** 학습 쪽에서 한 번 더 막아둔다 -- `model/data.py` 의 `TrackDataset`
이 `min_gen="gen2"` 기본값으로 `meta.gen_label` 을 보고 거른다
(`04_train.py --min-gen ""` 로 끌 수 있다).

### 생성기 세대 (`meta.gen`) — 학습 전에 반드시 볼 것
레코드마다 `meta: {gen, seed, port, ts}` 가 들어간다. `gen` 은 **그 레코드를 만든
생성기의 커밋**이다. 생성기를 고치면 같은 (bounds, lift, banked) 설정이라도
분포가 달라지므로, 합쳐놓고 나면 세대를 구분할 방법이 이것뿐이다.
작업 트리가 더러운 채로 수집하면 `+dirty` 가 붙는다 (커밋만으로 생성기를 특정
못 하므로).

2026-09-05~06 이틀에 세대가 넷 났다:

| 세대 | 커밋 | 무엇이 다른가 |
|---|---|---|
| gen1 | `07784aa` | **`banked` 플래그가 사실상 무효.** banked=True 인데도 맨턴 중앙 9개. 격렬도가 10을 한 번도 안 넘음 |
| gen2 | `b7d0a1e` | `strict_banked` 로 영역 분리 (맨턴 중앙 1개) + 리프트 10~13 개방으로 격렬도 상단 확보 |
| gen3 | `66939a4` | 설계에 시간 상한. 분포는 gen2 와 같고 수집 속도만 다름 |
| gen4 | `af04072` | 스테이션 플랫폼 타일을 점유로 반영. 트랙 꼬리가 플랫폼 위를 지나 게임이 배치를 거부하던 것 (하이브리드 경로 실측 60% -> 77%) |

**`--min-gen gen2` 가 기본이다.** gen1 은 `banked` 라벨이 오염돼 있어서 그 필드로
조건부 학습을 하면 안 된다. 시퀀스 자체는 유효하니 사전학습용으로는 쓸 수 있다.

`build_dataset.py` 상단의 `GENERATIONS` 표가 `meta` 가 없던 초기 파일들을
파일 이름으로 소급 태깅한다. **생성기를 또 고치면 그 표에 세대를 추가할 것.**
